'use client';

import { Standing } from '@/types';

type StandingsAndFixturesProps = {
  standingsData: Standing[];
};

const StandingsAndFixtures = () => {
  return <div>StandingsAndFixtures</div>;
};

export default StandingsAndFixtures;
